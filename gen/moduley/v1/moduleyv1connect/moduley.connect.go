// AiGen START

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: moduley/v1/moduley.proto

package moduleyv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "gen-poc/gen/moduley/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ModuleYServiceName is the fully-qualified name of the ModuleYService service.
	ModuleYServiceName = "moduley.v1.ModuleYService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ModuleYServiceComputeProcedure is the fully-qualified name of the ModuleYService's Compute RPC.
	ModuleYServiceComputeProcedure = "/moduley.v1.ModuleYService/Compute"
)

// ModuleYServiceClient is a client for the moduley.v1.ModuleYService service.
type ModuleYServiceClient interface {
	Compute(context.Context, *connect.Request[v1.ComputeRequest]) (*connect.Response[v1.ComputeResponse], error)
}

// NewModuleYServiceClient constructs a client for the moduley.v1.ModuleYService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewModuleYServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ModuleYServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	moduleYServiceMethods := v1.File_moduley_v1_moduley_proto.Services().ByName("ModuleYService").Methods()
	return &moduleYServiceClient{
		compute: connect.NewClient[v1.ComputeRequest, v1.ComputeResponse](
			httpClient,
			baseURL+ModuleYServiceComputeProcedure,
			connect.WithSchema(moduleYServiceMethods.ByName("Compute")),
			connect.WithClientOptions(opts...),
		),
	}
}

// moduleYServiceClient implements ModuleYServiceClient.
type moduleYServiceClient struct {
	compute *connect.Client[v1.ComputeRequest, v1.ComputeResponse]
}

// Compute calls moduley.v1.ModuleYService.Compute.
func (c *moduleYServiceClient) Compute(ctx context.Context, req *connect.Request[v1.ComputeRequest]) (*connect.Response[v1.ComputeResponse], error) {
	return c.compute.CallUnary(ctx, req)
}

// ModuleYServiceHandler is an implementation of the moduley.v1.ModuleYService service.
type ModuleYServiceHandler interface {
	Compute(context.Context, *connect.Request[v1.ComputeRequest]) (*connect.Response[v1.ComputeResponse], error)
}

// NewModuleYServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewModuleYServiceHandler(svc ModuleYServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	moduleYServiceMethods := v1.File_moduley_v1_moduley_proto.Services().ByName("ModuleYService").Methods()
	moduleYServiceComputeHandler := connect.NewUnaryHandler(
		ModuleYServiceComputeProcedure,
		svc.Compute,
		connect.WithSchema(moduleYServiceMethods.ByName("Compute")),
		connect.WithHandlerOptions(opts...),
	)
	return "/moduley.v1.ModuleYService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ModuleYServiceComputeProcedure:
			moduleYServiceComputeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedModuleYServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedModuleYServiceHandler struct{}

func (UnimplementedModuleYServiceHandler) Compute(context.Context, *connect.Request[v1.ComputeRequest]) (*connect.Response[v1.ComputeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("moduley.v1.ModuleYService.Compute is not implemented"))
}
// AiGen END
